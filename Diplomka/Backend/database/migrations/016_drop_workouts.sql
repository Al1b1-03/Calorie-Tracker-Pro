-- Удаляем старую таблицу тренировок (пересоздаём в 017)
-- ВНИМАНИЕ: скрипт удаления отключён, чтобы не терять данные при каждом запуске бэкенда.
-- Если нужно один раз пересоздать таблицу workouts, временно раскомментируйте строки ниже,
-- выполните миграции и снова закомментируйте.
--DROP INDEX IF EXISTS idx_workouts_title;
--DROP TABLE IF EXISTS workouts CASCADE;
